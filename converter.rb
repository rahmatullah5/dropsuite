#!/usr/bin/env ruby
class Converter
  def initialize(folder_destination)
    @folder_destination = folder_destination.chomp.strip
  end
  
  def call
    #TODO: Add main function to count on how many files inside a path with the same content.
  end
end